# AI Prompt Library

## Generate New Feature

```
Using our W-stack (React + TypeScript + Fastify + PostgreSQL), generate:

[Feature description]

Requirements:
- Frontend: React Hook Form + Zod validation
- Backend: Fastify route with validation
- Database: SQL migration if needed
- Styling: Tailwind CSS
- Testing: Tests for complex logic

Create:
1. Frontend component
2. Backend API route
3. Zod schema in shared/schemas/
4. Database migration if needed
5. Tests
```

## Fix Bug

```
I have a bug in [component/file name]:

**Symptoms:** [what's happening]
**Expected:** [what should happen]
**Error:** [error message]

Context: W-stack (React + TypeScript + Fastify)

Please:
1. Identify root cause
2. Suggest fix
3. Generate test to prevent regression
```